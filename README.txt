Vodafone Flexbench

An ionic application


/// Inserion method into IndexedDB
 // csvToJsonProvider.insertDatatoDB({"RequestDepartment":"TYI","ID":"40",
      //             "Name": "PPM-ID 178161 - CCS Relaunch-Projekt - Phase 1 und 2123"}).then(data =              
                
      //               });  

/// Selection method from IndexedDB 
   // csvToJsonProvider.selectDataFromIndexedDB({"ID":"40"}).then(data => {
      //  });  

      Update method for IndexedDB
      {"RequestDepartment":"TI"} = Query String
      {"ItemName":"TestrajItem","Name":"Rajdeep"} = object attribute to be updated with
      // CsvToJson.updateDataFromIndexedDB([{"RequestDepartment":"TI"},{"ItemName":"TestrajItem","Name":"Rajdeep"}]).then(data => {
      //   this.testvar = data;
      //   var objects  = JSON.parse(this.testvar);
      //             objects.forEach(function(obj) {
      //                       console.log("inside inside " + obj); 
      //                       CsvToJson.insertDatatoDB(obj).then(data => {
          
      //                       });   

      //             });
      // }); 